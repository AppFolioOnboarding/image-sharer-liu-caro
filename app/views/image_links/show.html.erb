<img style='max-width:400px' src=<%=@image_link.url%> />
<p>Tags:
  <%= @image_link.tag_list%>
</p>

<div>
  <%= button_to 'Delete image', @image_link, :method => 'delete', data: {:confirm => 'Are you sure?' } %>
</div>

<div>
  <%= link_to "Back", root_path %>
</div>
